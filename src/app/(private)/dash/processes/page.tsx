import { ProcessesView } from '@/modules/process/view/processes-view'

/**
 * Page-level wrapper that renders the ProcessesView component.
 *
 * Renders the main ProcessesView for the page.
 *
 * @returns The JSX element for the page's processes view.
 */
export default function Page() {
	return <ProcessesView />
}
